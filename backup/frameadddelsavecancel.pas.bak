unit FrameAddDelEditSaveCancel;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, FileUtil, Forms, Controls, ExtCtrls, Buttons;

type

  { TFraSaveCancelAddDelete }

  TFraSaveCancelAddDelete = class(TFrame)
    BtnEdit: TBitBtn;
    BtnDelete: TBitBtn;
    BtnAdd: TBitBtn;
    BtnCancel: TBitBtn;
    BtnSave: TBitBtn;
    PanBottom: TPanel;
  private
    { private declarations }
  public
    { public declarations }
  protected
		procedure Loaded; override;
  end;

implementation

{$R *.lfm}

uses
  FormMain;

procedure TFraSaveCancelAddDelete.Loaded;
begin
  inherited;
  FrmMain.ImgLstBtn.GetBitmap(3, BtnSave.Glyph);
  FrmMain.ImgLstBtn.GetBitmap(2, BtnCancel.Glyph);
  FrmMain.ImgLstBtn.GetBitmap(11, BtnAdd.Glyph);
  FrmMain.ImgLstBtn.GetBitmap(10, BtnDelete.Glyph);
end;
end.

